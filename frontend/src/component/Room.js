import React from 'react';

const Room = ({ room }) => {
  return (
    <div className="room-container">
      {/* Render room details */}
      {/* Render conversation */}
      {/* Render comment form */}
      {/* Render participants */}
    </div>
  );
};

export default Room;

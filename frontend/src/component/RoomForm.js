import React from 'react';

const RoomForm = ({ form }) => {
  return (
    <div>
      <form method="POST" action="">
        {/* Render room form fields */}
        <input type="submit" value="Submit" />
      </form>
    </div>
  );
};

export default RoomForm;

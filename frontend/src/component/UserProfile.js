import React from 'react';

const UserProfile = ({ user }) => {
  return (
    <div style={{ display: 'flex' }}>
      {/* Render user profile */}
      {/* Render topic_component, feed_component, and recent_activity */}
    </div>
  );
};

export default UserProfile;
